class Hello {
    constructor(startName) {
        this.value = startName
    }

    hi() {
        this.value = ('Hola  ') + this.value
        return this.value
    }
}

const hello = new Hello('astrid')

console.log(hello.hi())
